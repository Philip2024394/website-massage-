fund=false
audit=false
# Skip optional dependencies to avoid rollup native binary issues
optional=false
# Force Rollup to use JavaScript fallback instead of native binaries
ROLLUP_NO_NATIVE=true
npm_config_build_from_source=true