# ABSOLUTE MINIMUM CONFIG
[build]
  publish = "dist"
  command = "corepack enable pnpm && pnpm install && pnpm run build:simple"

# SPA redirect
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
