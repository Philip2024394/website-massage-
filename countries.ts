
export const COUNTRIES = [
    // Indonesia market only
    { name: "Indonesia", code: "ID", dial_code: "+62" },
];

// Default coordinates for Indonesia
export const COUNTRY_DEFAULT_COORDS: Record<string, { lat: number; lng: number; city: string }> = {
    ID: { lat: -6.200000, lng: 106.816666, city: 'Jakarta' },
};
