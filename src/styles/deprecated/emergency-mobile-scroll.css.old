/* 🚨 EMERGENCY MOBILE SCROLL FIX - SIMPLEST POSSIBLE */

/* Reset everything to allow natural scrolling */
html {
  height: 100%;
  overflow-y: auto !important;
  overflow-x: hidden !important;
  -webkit-overflow-scrolling: touch;
}

body {
  height: 100%;
  overflow-y: auto !important;
  overflow-x: hidden !important;
  -webkit-overflow-scrolling: touch;
  position: relative !important;
  touch-action: pan-y pinch-zoom !important;
}

/* Root container should not restrict height */
#root {
  min-height: 100%;
  height: auto !important;
  overflow: visible !important;
  position: relative;
}

/* Mobile specific - kill all height restrictions */
@media (max-width: 768px) {
  * {
    -webkit-overflow-scrolling: touch !important;
  }
  
  /* Allow body to scroll freely */
  body {
    position: relative !important;
    overflow-y: auto !important;
    height: auto !important;
    min-height: 100vh !important;
  }
  
  /* Remove fixed heights that prevent scrolling */
  .h-screen,
  .min-h-screen,
  [class*="h-screen"],
  [class*="h-dvh"],
  [class*="h-vh"] {
    height: auto !important;
    min-height: auto !important;
  }
  
  /* Main containers must allow content to flow */
  main,
  .page-container,
  .landing-page-container {
    height: auto !important;
    min-height: auto !important;
    max-height: none !important;
    overflow: visible !important;
  }
}
