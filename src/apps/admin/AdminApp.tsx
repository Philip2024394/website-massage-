// Legacy admin app placeholder: admin is now a separate app
export default function AdminApp() {
    return null;
}

/**
 * Standalone Admin App
 * 100% separated from main customer app
 * Uses same Appwrite backend for data
 */
// See apps/admin-dashboard for the active admin application
