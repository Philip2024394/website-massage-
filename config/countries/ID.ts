import type { CountryAppConfig } from './defaults';

const ID: CountryAppConfig = {
  code: 'ID',
  name: 'Indonesia',
  currencyCode: 'IDR',
  currencyLocale: 'id-ID',
  features: {
    agents: true,
    commissions: true,
    marketplace: true,
  },
};

export default ID;
