// Proper ending structure for fetchTherapistData function
// This should replace lines 570-584 in TherapistDashboardPage.tsx

            setCoordinates({ lat: 0, lng: 0 });
            setStatus(AvailabilityStatus.Offline);
            setIsLicensed(false);
            setLicenseNumber('');
        }
        
        // Mark data as loaded after processing
        setDataLoaded(true);
        
    } catch (error) {
        console.error('❌ Error fetching therapist data:', error);
        setDataLoaded(true);
    } finally {
        setIsLoading(false);
    }
}, [therapistId, existingTherapistData]);