// Corrupted section removed from TherapistDashboardPage.tsx around lines 570-585
// Problem: Missing try block opening, extra closing brace, incorrect structure

            setCoordinates({ lat: 0, lng: 0 });
            setStatus(AvailabilityStatus.Offline);
            setIsLicensed(false);
            setLicenseNumber('');
        }
            
            setDataLoaded(true);
            
        } catch (error) {
            console.error('❌ Error fetching therapist data:', error);
            setDataLoaded(true);
        } finally {
            setIsLoading(false);
        }
    }, [therapistId, existingTherapistData]);