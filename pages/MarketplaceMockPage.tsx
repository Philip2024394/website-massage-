// Mock page removed. Keep a minimal noop component to avoid accidental imports breaking build.
import React from 'react';

const MarketplaceMockPage: React.FC = () => {
  return null;
};

export default MarketplaceMockPage;
